# ============================================================
# Federated Transformer for 6G-ISAC — Python Dependencies
# ============================================================
# NOTE: torch and torchvision are installed separately in the
# Dockerfile using the CPU-only wheel to avoid the ~2 GB CUDA build.
# If running locally WITH a GPU, install manually:
#   pip install torch torchvision
# ============================================================

# Federated learning
flwr>=1.5.0

# Numerical / ML utilities
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0

# API server (optional FastAPI endpoints)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Dashboard
streamlit>=1.28.0

# Visualisation
matplotlib>=3.7.0
plotly>=5.15.0

# Utilities
tqdm>=4.65.0
